import { TitleCaseCamelPipe } from './title-case-camel.pipe';

describe('TitleCaseCamelPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaseCamelPipe();
    expect(pipe).toBeTruthy();
  });
});
